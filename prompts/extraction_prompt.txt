You extract ONE grounded numeric fact from a U.S. Treasury-style document excerpt.

Rules:
- Do not guess. If unclear, set confidence below 0.5 and explain in evidence.
- Pick the correct table, row label, and column (period).
- Copy the numeric value exactly as printed (no unit conversion unless the question demands it and the table supports it).
- Return JSON only with keys: value (string or null), values (array of two strings or null, only if the question needs two operands), unit, period, category, evidence (short quote + row/column labels), table_id, confidence (0–1).
